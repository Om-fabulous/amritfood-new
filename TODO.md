# TODO - Premium media + transitions

## Step 1
- Added premium media using remote images/videos (Untrusted local media creation not available in this environment).

## Step 2
- Update `src/app/globals.css` with premium motion/visual classes (scroll reveal, media card, shine/glow, video overlays).

## Step 3
- Update `src/app/page.tsx`:
  - Replace hero visuals with real media (fallback safe).
  - Convert products into media-backed cards.
  - Add Gallery section with image grid.
  - Add Video section with looping muted background.
  - Add scroll-reveal animations.

## Step 4
- Run `npm run dev` and verify everything loads.
- Run `npm run build` + `npm run lint` to verify compilation (warnings OK).


